# Design: Micro SaaS landing page roast agent

## Architecture
The first draft should be a minimal local application or script that proves the main workflow without external deployment.

## Data
Use sample or local data by default. Avoid paid APIs until explicitly approved.

## Risks
- The POC may prove interface feasibility without proving market demand.
- External integrations may need a second design pass.
